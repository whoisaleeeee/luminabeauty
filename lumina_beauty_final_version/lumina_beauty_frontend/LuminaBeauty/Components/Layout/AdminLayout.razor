@inherits LayoutComponentBase

@using LuminaBeauty.Services

@inject AuthService Auth
@inject NavigationManager Navigation

<div class="admin-shell">

    <aside class="admin-sidebar">

        <div class="admin-logo-section">
            <div class="admin-logo-mark">
                <img src="/images/logo-lumina.png"
                     alt="Lumina Beauty"
                     onerror="this.style.display='none'; this.parentElement.classList.add('logo-fallback');" />
                <span>LB</span>
            </div>

            <div class="admin-logo-text">
                <strong>Lumina Beauty</strong>
                <small>Portal Administrativo</small>
            </div>
        </div>

        <div class="admin-menu-label">GESTIÓN</div>

        <nav class="admin-nav">

            <NavLink href="/admin/catalogo" class="admin-nav-link">
                <i class="bi bi-grid-1x2"></i>
                <span>Catálogo</span>
            </NavLink>

            <NavLink href="/admin/inventario" class="admin-nav-link">
                <i class="bi bi-box-seam"></i>
                <span>Inventario</span>
            </NavLink>

            <NavLink href="/admin/pedidos" class="admin-nav-link">
                <i class="bi bi-bag-check"></i>
                <span>Pedidos</span>
            </NavLink>

            <NavLink href="/admin/reclamos" class="admin-nav-link">
                <i class="bi bi-chat-square-text"></i>
                <span>Reclamos</span>
            </NavLink>

            <NavLink href="/admin/reportes" class="admin-nav-link">
                <i class="bi bi-bar-chart-line"></i>
                <span>Reportes</span>
            </NavLink>

            <div class="admin-menu-label admin-menu-label-secondary">SISTEMA</div>

            <NavLink href="/admin/configuracion" class="admin-nav-link">
                <i class="bi bi-gear"></i>
                <span>Configuración</span>
            </NavLink>

            <NavLink href="/admin/perfil" class="admin-nav-link">
                <i class="bi bi-person-circle"></i>
                <span>Mi perfil</span>
            </NavLink>
        </nav>

        <div class="admin-sidebar-footer">

            <button type="button" class="admin-help-button">
                <i class="bi bi-question-circle"></i>
                <span>Centro de ayuda</span>
            </button>

            <button type="button"
                    class="admin-logout-button"
                    @onclick="CerrarSesionAsync">
                <i class="bi bi-box-arrow-right"></i>
                <span>Cerrar sesión</span>
            </button>
        </div>

    </aside>

    <main class="admin-main">
        @Body
    </main>

</div>

@code {
    private async Task CerrarSesionAsync()
    {
        await Auth.LogoutAsync();
        Navigation.NavigateTo("/login", true);
    }
}